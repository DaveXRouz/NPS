# Frontend Environment Configuration
# Copy to .env and fill in values

# API key for authenticating frontend requests to the API
# Generate one via POST /api/auth/api-keys after creating an admin user
VITE_API_KEY=
